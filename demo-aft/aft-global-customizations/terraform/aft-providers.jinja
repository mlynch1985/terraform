## © 2024 Amazon Web Services, Inc. or its affiliates. All Rights Reserved.
## This AWS Content is provided subject to the terms of the AWS Customer Agreement available at
## http://aws.amazon.com/agreement or other written agreement between Customer and either
## Amazon Web Services, Inc. or Amazon Web Services EMEA SARL or both.

## Auto generated providers.tf ##
## Updated on: {{ timestamp }} ##

provider "aws" {
  region = "{{ provider_region }}"
  assume_role {
    role_arn    = "{{ target_admin_role_arn }}"
  }
  default_tags {
    tags = {
      managed_by = "AFT"
    }
  }
}

provider "aws" {
  region  = "{{ provider_region }}"
  alias   = "ct-management"
  profile = "ct-management"
  default_tags {
    tags = {
      managed_by = "AFT"
    }
  }
}

provider "aws" {
  region  = "{{ provider_region }}"
  alias   = "aft-management"
  profile = "aft-management"
  default_tags {
    tags = {
      managed_by = "AFT"
    }
  }
}

# AWSAFTExecution in audit account used to define Service Automation IAM Roles from the Audit Account
provider "aws" {
  region  = "{{ provider_region }}"
  alias   = "ct-audit"
  profile = "ct-audit"
  default_tags {
    tags = {
      managed_by = "AFT"
    }
  }
}

provider "aws" {
  region = "us-east-1"
  alias  = "region1"
  assume_role {
    role_arn    = "{{ target_admin_role_arn }}"
  }
  default_tags {
    tags = {
      managed_by = "AFT"
    }
  }
}

provider "aws" {
  region = "us-east-2"
  alias  = "region2"
  assume_role {
    role_arn    = "{{ target_admin_role_arn }}"
  }
  default_tags {
    tags = {
      managed_by = "AFT"
    }
  }
}

provider "aws" {
  region = "us-west-2"
  alias  = "region3"
  assume_role {
    role_arn    = "{{ target_admin_role_arn }}"
  }
  default_tags {
    tags = {
      managed_by = "AFT"
    }
  }
}
